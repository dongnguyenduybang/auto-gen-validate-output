// import { createApiValidator } from "./api-validator";
// import { EventContext, TestContext, WSSContext } from "./text-context";

// export async function executeWSSCheck(wssCheck: any, globalContext: TestContext, globalWSSContext: WSSContext, eventContext: EventContext) {
//     const validator = createApiValidator(globalContext);
//     const errors: Array<{ eventIndex: number; event; errors: any[] }> = [];
//     let matchedActorEvents = 0;
//     let matchedRecipientEvents = 0;
//     const wsActor = globalWSSContext.getValue();
//     const wsRecipient = globalWSSContext.getValue();
// }